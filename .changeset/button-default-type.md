---
"@jects/jds": minor
---

**Button**

`BlockButton` / `LabelButton` / `IconButton`의 기본 `type`을 `button`으로 지정했습니다. 네이티브 `<button>`이 폼 내부에서 `submit`을 기본값으로 삼아 의도치 않은 제출이 발생하는 문제를 방지합니다. `type` prop으로 오버라이드할 수 있습니다.
